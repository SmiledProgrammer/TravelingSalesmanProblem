##############################
# Traveling Salesman Problem #
# Configuration file         #
##############################

# Population size
# (as an integer)
population_size = \
    30

# Number of iterations
# (as an integer)
iterations = \
    200

# Probability of crossover of a chromosome
# (value range: <0, 1>)
crossover_probability = \
    0.5

# Probability of mutation of a chromosome
# (value range: <0, 1>)
mutation_probability = \
    0.25

# Cities coordinates
# (as a tuple of tuples)
cities_coordinates = (
        (2,3), (9,9), (6,7), (1,9), (3,8), (5,8), (4,4), (10,6), (7,5), (8,2)
    )
